# ===========================================================================
# CI 可安装的核心依赖（不含需要本地软件的包）
# ===========================================================================

# OpenManus 框架
git+https://github.com/FoundationAgents/OpenManus.git

# 有限元分析（核心）
openseespy>=3.5.0
numpy>=1.24.0
scipy>=1.10.0

# CAD 绘图
ezdxf>=1.1.0

# 知识库
langchain>=0.1.0
chromadb>=0.4.0
openai>=1.0.0
PyPDF2>=3.0.0

# 配置文件解析
toml>=0.10.2

# 数据处理
pandas>=2.0.0
pydantic>=2.0.0

# 可视化
matplotlib>=3.7.0
plotly>=5.14.0

# 测试
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# 工具
python-dotenv>=1.0.0
pyyaml>=6.0

# ===========================================================================
# 以下包需要本地环境，CI 中跳过：
#   ansys-mapdl-core  — 依赖本地 Ansys 安装
#   ifcopenshell      — 平台兼容性问题
#   specklepy         — BIM 导出（可选功能）
# ===========================================================================
