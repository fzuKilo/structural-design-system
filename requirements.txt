# ===========================================================================
# CI 可安装的核心依赖（不含需要本地软件的包）
# ===========================================================================

# OpenManus 框架（在 Dockerfile 中单独通过镜像安装）

# 有限元分析（核心）
openseespy>=3.5.0
numpy>=1.24.0
scipy>=1.10.0

# CAD 绘图
ezdxf>=1.1.0

# 知识库
langchain>=0.1.0
chromadb>=0.4.0
openai>=1.0.0
PyPDF2>=3.0.0

# 配置文件解析
toml>=0.10.2

# 数据处理
pandas>=2.0.0
pydantic>=2.0.0
pydantic[email]>=2.0.0

# 可视化
matplotlib>=3.7.0
plotly>=5.14.0

# 测试
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# 工具
python-dotenv>=1.0.0
pyyaml>=6.0

# BIM 导出
specklepy==2.19.6
ifcopenshell==0.8.5

# ===========================================================================
# 以下包需要本地环境，CI 中跳过：
#   ansys-mapdl-core  — 依赖本地 Ansys 安装
# ===========================================================================

# 数据库
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# 加密
cryptography>=41.0.0

# 版本锁定：防止 mcp/chromadb 将 starlette 升级到不兼容 FastAPI 0.109.0 的版本
starlette==0.35.0
