[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Exclude interactive tests that require user input
addopts = -v --tb=short -k "not (stage7 or stage8)"
markers =
    standard_case: 标准算例有限元校验测试（解析解/Ansys参考值）
